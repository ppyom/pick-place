import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as DividerStories from './stories';

<Meta of={DividerStories} />

# Divider

공통 구분선 컴포넌트입니다. `horizontal` / `vertical` 방향을 지원합니다.

<Canvas of={DividerStories.Horizontal} />
<Canvas of={DividerStories.Vertical} />

## Props

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>orientation</td>
      <td>
        <code>'horizontal' | 'vertical'</code>
      </td>
      <td>
        <code>'horizontal'</code>
      </td>
      <td>구분선 방향입니다.</td>
    </tr>
    <tr>
      <td>className</td>
      <td>
        <code>string</code>
      </td>
      <td>-</td>
      <td>추가 클래스입니다.</td>
    </tr>
  </tbody>
</table>

## 사용 시 주의

- `vertical`일 때는 부모 컨테이너에 명시적 높이나 `items-stretch`가 있어야 `self-stretch`가 의도대로 동작합니다.
